package game.pieces;

import game.pieces.Piece;
import javafx.scene.paint.Color;

public class Bishop extends Piece{
    public Bishop(Color _color, int _x, int _y) {
        super("b", "bishop",_color, _x, _y);
    }

    @Override
    public void move() {

    }
}
